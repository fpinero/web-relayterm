apiVersion: v1
kind: Service
metadata:
  name: relayterm-landing
  namespace: relayterm
  labels:
    app.kubernetes.io/name: relayterm-landing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relayterm-landing
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
